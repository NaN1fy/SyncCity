from enum import Enum

class SensorType(Enum):
  """
    Class that represents an enumeration of possible types of a sensor (e.g.
    temperature, humidity, wind etc...), in
    the form of SENSOR_TYPE = "sensor_type"
  """
  
    TEMPERATURE = "temperature"
